{
  "name": "weather-station-backend",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "scripts": {
    "presst:start": "stacks/migrations/esbuild.config.js",
    "sst:start": "AWS_PROFILE=klic-dev sst start --stage dev",
    "presst:build": "stacks/migrations/esbuild.config.js",
    "sst:build": "AWS_PROFILE=klic-dev sst build --stage dev",
    "presst:deploy": "stacks/migrations/esbuild.config.js",
    "sst:deploy": "AWS_PROFILE=klic-dev sst deploy --stage dev",
    "sst:remove": "AWS_PROFILE=klic-dev sst remove --stage dev",
    "sst:console": "AWS_PROFILE=klic-dev sst console --stage dev",
    "prepare": "husky install",
    "prettier": "prettier --write --ignore-unknown"
  },
  "workspaces": [
    "backend",
    "frontend/*"
  ],
  "eslintConfig": {
    "extends": [
      "serverless-stack"
    ]
  },
  "devDependencies": {
    "@serverless-stack/cli": "0.65.4",
    "@serverless-stack/resources": "0.65.4",
    "@tsconfig/node14": "^1.0.1",
    "@types/aws-lambda": "^8.10.92",
    "@types/node": "<15.0.0",
    "aws-cdk-lib": "2.7.0",
    "husky": "7.0.4",
    "jest": "^27.5.1",
    "lerna": "4.0.0",
    "lint-staged": "12.3.4",
    "prettier": "2.5.1",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "kysely": "^0.17.1",
    "pg": "^8.7.3"
  }
}
