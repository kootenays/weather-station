export const DeviceDetailPage: React.FC = () => {
  return (
    <div>
      This is the detail page for the device... Should have,
      <ul>
        <li>Default details, e.g. name, etc.</li>
        <li>
          Sensor Data, e.g. all th sensor values coming through, should have a
          refresh button to check latest value for that sensor
        </li>
      </ul>
    </div>
  );
};
